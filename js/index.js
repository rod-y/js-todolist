const age = prompt("how old are you");

if (age >= 18 && age <= 21) {
  document.write('you can drink but you should not')
} else if(age > 21){
  document.write('go ahead')
} else{
  document.write('too young')
}
//  || = or , && = and

//  || = or , && = and
